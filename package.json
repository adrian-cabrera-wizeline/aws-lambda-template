{
  "name": "lambda-backend-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "node esbuild.config.js",
    "db:start": "docker-compose -f infra-local/docker-compose.yml up -d",
    "db:stop": "docker-compose -f infra-local/docker-compose.yml down",
    "db:seed": "npx ts-node infra-local/seed/seed-dynamo.ts",

    "dev:env": "npm run db:start && echo '⏳ Waiting for DBs...' && node -e \"setTimeout(() => {}, 10000)\" && npm run db:seed",
    
    "test": "npx jest",
    "test:runner": "node scripts/test-runner.mjs",
    "test:unit": "npx jest --testPathPattern=src",

    "----- LOCAL DEV (SAM Points to Dist) -----": "",
    "// NOTE": "We build FIRST, then point SAM to the 'dist' folder to match Production behavior",

    "local:api": "npm run build && sam local start-api -t infra-local/local-debug.yaml --env-vars env.json --docker-network infra-local_default -d 5858",
    "invoke:price:create": "npm run build && sam local invoke PriceFetcherFunction -t infra-local/local-debug.yaml -e events/price-fetcher/create.json --env-vars env.json --docker-network infra-local_default",
    "invoke:price:get": "npm run build && sam local invoke PriceFetcherFunction -t infra-local/local-debug.yaml -e events/price-fetcher/read.json --env-vars env.json --docker-network infra-local_default",
    "invoke:price:update": "npm run build && sam local invoke PriceFetcherFunction -t infra-local/local-debug.yaml -e events/price-fetcher/update-price.json --env-vars env.json --docker-network infra-local_default",
    "invoke:price:delete": "npm run build && sam local invoke PriceFetcherFunction -t infra-local/local-debug.yaml -e events/price-fetcher/recall-product.json --env-vars env.json --docker-network infra-local_default"

  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^1.18.1",
    "@aws-lambda-powertools/metrics": "^1.18.1",
    "@aws-lambda-powertools/tracer": "^1.18.1",
    "@aws-sdk/client-dynamodb": "^3.400.0",
    "@aws-sdk/lib-dynamodb": "^3.400.0",
    "@middy/core": "^4.6.0",
    "@middy/http-error-handler": "^7.0.2",
    "@middy/http-header-normalizer": "^4.6.0",
    "@middy/http-json-body-parser": "^4.6.0",
    "oracledb": "^6.0.0",
    "uuid": "^13.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.19.30",
    "@types/oracledb": "^6.10.1",
    "chalk": "^5.6.2",
    "esbuild": "^0.27.2",
    "fx": "^39.2.0",
    "glob": "^10.3.3",
    "inquirer": "^8.2.7",
    "jest": "^29.6.2",
    "pino-pretty": "^13.1.3",
    "shelljs": "^0.10.0",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
